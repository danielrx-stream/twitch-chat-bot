import {MessageAndData} from '../utils';

export const args = [];

export const handler = async({say}: MessageAndData) => {
    say(`Install BTTV: https://betterttv.com and FFZ: https://www.frankerfacez.com/ so you can see all the emotes in chat!`);
}