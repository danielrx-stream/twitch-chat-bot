import {MessageAndData} from '../utils';

export const args = [];

export const handler = async({say}: MessageAndData) => {
    say(`Join the discord: https://danielrx.dev/discord and suggest new ideas for streams or just hang out with us! PogChamp PogChamp PogChamp`);
}