import {MessageAndData} from '../utils';

export const args = [];

export const handler = async({say}: MessageAndData) => {
    say(`Shadow is my 9 month old German Shepherd Dog I've had for about 5 months now widepeepoHappy`);
}